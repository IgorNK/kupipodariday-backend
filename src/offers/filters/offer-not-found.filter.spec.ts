import { OfferNotFoundExceptionFilter } from './offer-not-found.filter';

describe('OfferNotFoundExceptionFilter', () => {
  it('should be defined', () => {
    expect(new OfferNotFoundExceptionFilter()).toBeDefined();
  });
});
