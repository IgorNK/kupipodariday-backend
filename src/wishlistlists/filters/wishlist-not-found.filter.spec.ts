import { WishlistNotFoundExceptionFilter } from './wishlist-not-found.filter';

describe('WishlistNotFoundExceptionFilter', () => {
  it('should be defined', () => {
    expect(new WishlistNotFoundExceptionFilter()).toBeDefined();
  });
});
